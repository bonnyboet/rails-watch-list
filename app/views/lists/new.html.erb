<%= simple_form_for(@list) do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.button :submit %>
<% end %>

<div class="card">
  <%= link_to 'Back to lists', lists_path(@list) %>
</div>
